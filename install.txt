1 - clone o projeto;
2 - crie um banco de dados mysql collation utf8_unicode_ci;
3 - crie um arquivo na raiz do projeto .env e preencha com as informações de conexao com o banco de dados;
4 - através do terminal na pasta do projeto rode o comando composer install;
5 - altere o arquivo cfp/app/providers/AuthServiceProvider.php

- comente da linha 30 até a linha 50; 

- insira esse codigo no fim do metodo: 
	
   parent::registerPolicies($gate);

- depois de rodar o item 7 descomente as linhas e comente as linhas que eu pedi pra vc comentar;

6 - rode o comando: php artisan key:generate;

7 - rode o comando: php artrisan migrate;

8 - rode o comando: php artisan serve

9 - abra o navegador: http://localhost:8000

Observação: pode ocorrer um erro de cannot driver, ai é só vc installar o drive do mysql na sua maquina.
 
